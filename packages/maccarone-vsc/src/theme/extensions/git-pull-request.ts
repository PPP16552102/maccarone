import { ThemeContext } from "@/types";

export default function colors(ctx: ThemeContext) {
  return {
    "issues.closed": "red",
    "issues.newIssueDecoration": "red",
    "issues.open": "red",
    "pullRequests.closed": "red",
    "pullRequests.draft": "red",
    "pullRequests.merged": "red",
    "pullRequests.notification": "red",
    "pullRequests.open": "red",
  };
}
